[package]
name = "lean-goalview"
version = "0.1.0"
edition = "2021"
description = "Lean 4 goal view for editors without an infoview: LSP proxy that renders goal state into a live-updating file"
license = "MIT"

[dependencies]
serde_json = "1"

[profile.release]
strip = true
